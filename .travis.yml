language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv install --dev
  - pipenv shell
# command to run tests
script:
  - pipenv run python -m pytest ./tests
